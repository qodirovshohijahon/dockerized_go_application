apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-app
  labels:
    app: go
spec:
  replicas: 4
  selector:
    matchLabels:
      app: go
  template:
    metadata:
      labels:
        app: go
    spec:
      containers:
      - name: go_application
        image: nginx:1.14.2
        ports:
        - containerPort: 80